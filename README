# UnityInputLagDemo

This is a demonstration of the 20-100ms input delay visible _only_ in built application for Mac OSX on unity. Even using the editor on a mac the "play" window displays no input lag, whereas built applications experience a visible input delay at 60fps.
